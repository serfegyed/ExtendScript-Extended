/**
 * Checks whether a value is a project Map instance.
 *
 * @param {*} value - The value to inspect.
 * @return {Boolean} True for Map instances.
 */
Map.isMap = function (value) {
    return value instanceof Map;
};
